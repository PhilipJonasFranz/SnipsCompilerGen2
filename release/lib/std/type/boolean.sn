#include<string.hn>

namespace Boolean {

	shared bool parseBool(char* str) {
		auto str0 = "true";
		if (equals(str, &str0)) return true;
		else return false;
	}
	
	shared char* toString(bool b) {
		if (b) return init<>("true");
		else return init<>("false");
	}

} 
