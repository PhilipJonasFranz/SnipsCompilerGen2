/*
 * Reserves a memory section using resv, assigns the given value
 * to this memory section, and returns a pointer to the new memory
 * section.
 */
shared T* init<T>(T value) {
	T* p = resv(sizeof(T));
	*p = value;
	return p;
}
