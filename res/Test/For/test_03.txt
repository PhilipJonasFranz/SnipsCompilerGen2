SOURCE
int main(int a) {
    int c = 0;
	for (int i = 0; i < a; i = i + 1) {
        c = c + i + 1;
		if (i < 5) {
			for (int b = 0; b < 4; b = b + 1) {
				c = c + c;
			}
		}
	}
	
    return c;
}
TESTCASES
3 4656
OUTPUT
/* --test_03.txt */
main:                                        /* Function: main */
    push { r3, r4, r5, r6 }
    mov r3, r0
    mov r4, #0
    mov r5, #0
main.L1: 
    cmp r5, r3
    bge main.L2
    add r1, r4, r5
    add r4, r1, #1
    cmp r5, #5
    bge main.L7
    mov r0, #0                               /* Evaluate Expression */
    mov r6, #0
main.L6: 
    cmp r6, #4
    bge main.L7
    add r4, r4, r4
    add r6, r6, #1
    b main.L6
main.L7: 
    add r5, r5, #1
    b main.L1
main.L2: 
    mov r0, r4                               /* Evaluate Expression */
    pop { r3, r4, r5, r6 }
    bx lr
    
