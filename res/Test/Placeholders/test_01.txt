SOURCE
struct Test {
	int [2] x;
}

int main(int k) {
	Test t = Test::(...);
	
	t.x [1] = 5;
	
	return t.x [1];
}
TESTCASES
5
OUTPUT
/* --test_24.txt */
main:                                        /* Function: main */
    sub sp, sp, #8                           /* Evaluate Expression */
    mov r0, #1
    push { r0 }
    mov r0, #5                               /* Evaluate Expression */
    str r0, [sp, #8]                         /* Store value to struct field */
    ldr r0, [sp, #8]                         /* Load field from struct */
    bx lr
    
