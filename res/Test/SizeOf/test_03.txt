SOURCE
int main(int x) {
	int* p = &x;
	return sizeof(p);
}
TESTCASES
1
OUTPUT
/* --test_03.txt */
main:                                        /* Function: main */
    mov fp, sp
    push { r0 }                              /* Push declaration on stack, referenced by addressof. */
    mov r0, #1                               /* Evaluate Expression */
    mov sp, fp
    bx lr
    
