SOURCE
int [2] [2] vec = {{3, 7}, {2, 0}};

int main() { 
	int [2] v0 = vec [0];
	return v0 [0];
}
TESTCASES
3
OUTPUT
/* --test_22.txt */
.data
vec: .skip 16
    
.text
/* Initialize the global variables */
    push { r0, r1, r2 }
    mov r0, #0
    mov r1, #2
    push { r1, r0 }
    mov r0, #7
    mov r1, #3
    push { r1, r0 }
    pop { r0 }
    ldr r1, .POOL0_main.L0_vec               /* Load from .data section */
    str r0, [r1, #0]
    pop { r0 }
    str r0, [r1, #4]
    pop { r0 }
    str r0, [r1, #8]
    pop { r0 }
    str r0, [r1, #12]
    pop { r0, r1, r2 }
main:                                        /* Function: main */
    ldr r1, .POOL0_main.L0_vec               /* Load data section address */
    ldr r0, [r1, #4]
    ldr r2, [r1, #0]
    push { r2, r0 }
    ldr r0, [sp, #0]
    bx lr
.POOL0_main.L0_vec: .word vec
    
