SOURCE
namespace Foo {
	namespace Baz {
		int x = 10;
	}
	
	struct Bar {
		int k;
	}
	
	int get(Bar bar) {
		return bar.k;
	}
	
}

int main() {
	Bar bar = Bar::(5);
	return get(bar) + x;
}

TESTCASES
15
OUTPUT
/* --test_04.txt */
.data
Foo.Baz.x: .word 10
HEAP_START: .word 0
    
.text
main:                         /* Function: main */
    mov fp, sp
    mov r0, #5                /* Evaluate Expression */
    push { r0 }
    sub r1, fp, #4            /* Evaluate Expression */
    ldr r0, [r1]              /* Load field from struct */
    mov r1, r0
    ldr r2, .L0_Foo.Baz.x     /* Load from .data section */
    ldr r2, [r2]
    add r0, r1, r2
    bx lr
    
.L0_Foo.Baz.x: .word Foo.Baz.x
.L1_HEAP_START: .word HEAP_START
