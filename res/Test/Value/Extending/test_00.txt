SOURCE
struct Base {
	int x;
}

struct Extend : Base {
	int y;
}

int main() {
	Base b = Base::(12);
	Extend e = Extend::(10, 15);
	return e.x + e.y + b.x;
}

TESTCASES
37
OUTPUT
.data
Base: .word 0
Extend: .word Base

.text
    b main
    
.global main
main:                                        /* Function: main */
    mov fp, sp
    mov r0, #12                              /* Evaluate Expression */
    push { r0 }
    ldr r0, .POOL0_Base
    push { r0 }
    mov r0, #15                              /* Evaluate Expression */
    mov r1, #10
    stmfd sp!, {r0, r1}
    ldr r0, .POOL0_Extend
    push { r0 }
    ldr r0, [fp, #-16]                       /* Load field from struct */
    ldr r2, [fp, #-12]                       /* Load field from struct */
    add r0, r0, r2
    ldr r2, [fp, #-4]                        /* Load field from struct */
    add r0, r0, r2
    bx lr
.POOL0_Extend: .word Extend
.POOL0_Base: .word Base
