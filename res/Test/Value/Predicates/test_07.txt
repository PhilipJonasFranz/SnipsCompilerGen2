SOURCE
bool sortPred(int x, int y) {
	return x < y;
}

int get(func (int, int) -> bool sp) {
	if (sp(5, 10)) 
		return 12;
	
	return 25;
}

int main() {
	/* Context check predicate at this location */
	return get(sortPred);
}
TESTCASES
12
OUTPUT
.version 4631715727

.include maybe __op_div.s
.include maybe __op_mod.s
.include maybe resv.s
.include maybe isa.s
.include maybe init.s
.include maybe hsize.s
.include maybe free.s

.data
lambda_get: .word get
lambda_sortPred: .word sortPred
NULL: .word 0
HEAP_START: .word 0

.text
    b main

/* System Routine, used to copy memory on the stack */
_routine_stack_copy_: 
    cmp r0, #0
    beq _routine_stack_copy_end_
    ldr r2, [r1, #-4]!
    sub r0, r0, #4
    push { r2 }
    b _routine_stack_copy_
_routine_stack_copy_end_: 
    mov pc, r10

.global sortPred
sortPred:                                    /* Function: sortPred */
    cmp r0, r1
    movlt r0, #1
    movge r0, #0
    bx lr

.global get
get:                                         /* Function: get */
    push { r3, lr }
    mov r3, r0
    mov r1, #10
    mov r0, #5
    add lr, pc, #8
    mov pc, r3
    cmp r0, #0
    beq get.L0
    mov r0, #12                              /* Evaluate Expression */
    ldmfd sp!, {r3, pc}
get.L0: 
    mov r0, #25                              /* Evaluate Expression */
    ldmfd sp!, {r3, pc}

.global main
main:                                        /* Function: main */
/* Context check predicate at this location */
    ldr r0, .P250521665_lambda_sortPred      /* Evaluate Expression */
    ldr r0, [r0]
    b get                                    /* Call get */
.P250521665_lambda_sortPred: .word lambda_sortPred
