SOURCE
int f(int x) {
	return x * x;
}

int map(int x, func int -> int pred) {
	return pred(x);
}

int main() {
	int p = (int) f;
	func pred0 = (func) p;

	int y = map(10, pred0);

	return y;
}
TESTCASES
100
OUTPUT
/* --test_04.txt */
    b main
lambda_f@0:                                  /* Function address getter for predication */
    add r0, pc, #8
    mov pc, r10
f@0:                                         /* Function: f */
    mul r0, r0, r0
    bx lr
    
map@2:                                       /* Function: map */
    push { r4, lr }
    mov r4, r1
    add lr, pc, #8
    mov pc, r4
    ldmfd sp!, {r4, pc}
    
main:                                        /* Function: main */
    add r10, pc, #8                          /* Evaluate Expression */
    b lambda_f@0
    mov r10, #0
    mov r1, r0
    mov r0, #10
    b map@2                                  /* Call map */
    
