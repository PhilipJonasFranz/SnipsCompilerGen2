SOURCE
int main() {
	int* p = null;
	if (p == null) return 10;
	else return 5;
}
TESTCASES
10
OUTPUT
.data
NULL: .word 0

.text
    b main
    
.global main
main:                                        /* Function: main */
    ldr r3, .POOL0_L0_NULL                   /* Evaluate Expression */
    ldr r1, .POOL0_L0_NULL                   /* Load null address */
    cmp r3, r1
    bne main.L1
    mov r0, #10                              /* Evaluate Expression */
    bx lr
main.L1: 
    mov r0, #5                               /* Evaluate Expression */
    bx lr
.POOL0_L0_NULL: .word NULL
