SOURCE
int [2] [2] vec = {{3, 7}, {2, 0}};

int main() { 
	int [2] v0 = vec [0];
	return v0 [0];
}
TESTCASES
3
OUTPUT
/* --test_22.txt */
.data
vec: .skip 16
    
.text
/* Initialize the global variables */
    sub sp, sp, #12
    stmea sp, {r0-r2}
    mov r0, #0
    mov r1, #2
    stmfd sp!, {r0, r1}
    mov r0, #7
    mov r1, #3
    stmfd sp!, {r0, r1}
    pop { r0 }
    ldr r1, .POOL0_L0_vec                    /* Load from .data section */
    str r0, [r1]
    pop { r0 }
    str r0, [r1, #4]
    pop { r0 }
    str r0, [r1, #8]
    pop { r0 }
    str r0, [r1, #12]
    ldmfd sp!, {r0-r2}
main:                                        /* Function: main */
    ldr r1, .POOL0_L0_vec                    /* Load data section address */
    ldr r0, [r1, #4]
    ldr r2, [r1]
    stmfd sp!, {r0, r2}
    ldr r0, [sp]
    bx lr
.POOL0_L0_vec: .word vec
    
