SOURCE
interface Inter<Z> {

	Z get<Z>();

}

struct X<T> : Inter<T> {

	T val;
	
	T get<T>() {
		return self->val;
	}

}

int main() {
	X<int> x1 = X<int>::(25);
	Inter<int> i1 = &x1;
	
	X<bool> x2 = X<bool>::(true);
	Inter<bool> i2 = &x2;
	
	return i1.get() + (int) i2.get();
}
TESTCASES
26
OUTPUT
/* --test_04.txt */
    b main
.L6_P0:                                      /* Interface : Inter, Provisos: INT */
    add r10, r12, #5
    mov r12, #0
    add pc, pc, r10
    b X.get_P3
    
X.get_P3:                                    /* Function: X.get, Provisos: INT | BOOL */
    mov r10, #0
    lsl r1, r0, #2                           /* Convert to bytes */
    ldr r0, [r1, #4]                         /* Load field from struct */
    bx lr
    
main:                                        /* Function: main */
    push { fp, lr }
    mov fp, sp
    mov r0, #25                              /* Evaluate Expression */
    push { r0 }
    mov r0, #1
    push { r0 }
    sub r0, fp, #8                           /* Evaluate Expression */
    lsr r3, r0, #2
    mov r0, #1                               /* Evaluate Expression */
    push { r0 }
    mov r0, #1
    push { r0 }
    sub r0, fp, #16                          /* Evaluate Expression */
    lsr r4, r0, #2
    mov r0, r3                               /* Evaluate Expression */
    bl .L6_P0                                /* Branch to relay table of Inter */
    mov r5, r0
    mov r0, r4                               /* Load parameters */
    bl .L6_P0                                /* Branch to relay table of Inter */
    add r0, r5, r0
    mov sp, fp
    ldmfd sp!, {fp, pc}
    
