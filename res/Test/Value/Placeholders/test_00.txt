SOURCE
struct Test {
	int [2] x;
}

int main(int k) {
	Test t = Test::(3...);
	return t.x [1];
}
TESTCASES
3
OUTPUT
.data
Test: .word 0

.text
    b main
    
.global main
main:                                        /* Function: main */
    mov fp, sp
    mov r0, #3                               /* Evaluate Expression */
    mov r1, #3
    ldr r2, .POOL0_Test
    stmfd sp!, {r0-r2}
    ldr r0, .POOL0_Test
    push { r0 }
    ldr r0, [fp, #-8]                        /* Load field from struct */
    mov sp, fp
    bx lr
.POOL0_Test: .word Test
