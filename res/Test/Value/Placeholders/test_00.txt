SOURCE
struct Test {
	int [2] x;
}

int main(int k) {
	Test t = Test::(3...);
	return t.x [1];
}
TESTCASES
3
OUTPUT
/* --test_00.txt */
main:                                        /* Function: main */
    mov fp, sp
    mov r0, #3                               /* Evaluate Expression */
    mov r1, #3
    mov r2, #1
    stmfd sp!, {r0-r2}
    mov r0, #1
    push { r0 }
    ldr r0, [fp, #-8]                        /* Load field from struct */
    mov sp, fp
    bx lr
    
