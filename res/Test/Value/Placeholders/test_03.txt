SOURCE
struct Test {
	int [2] x;
	bool k;
}

int main(int k) {
	Test t = Test::(...);
	
	t.x [1] = 5;
	t.k = true;
	
	return t.x [1] + (int) t.k;
}
TESTCASES
6
OUTPUT
/* --test_03.txt */
main:                                        /* Function: main */
    sub sp, sp, #12                          /* Evaluate Expression */
    mov r0, #1
    push { r0 }
    mov r0, #5                               /* Evaluate Expression */
    str r0, [sp, #8]                         /* Store value to struct field */
    mov r0, #1                               /* Evaluate Expression */
    str r0, [sp, #12]                        /* Store value to struct field */
    ldr r0, [sp, #8]                         /* Load field from struct */
    ldr r2, [sp, #12]                        /* Load field from struct */
    add r0, r0, r2
    bx lr
    
