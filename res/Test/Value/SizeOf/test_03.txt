SOURCE
int main(int x) {
	int* p = &x;
	return sizeof(p);
}
TESTCASES
1
OUTPUT
.data
.text
    b main
    
.global main
main:                                        /* Function: main */
    push { fp }
    mov fp, sp
    push { r0 }                              /* Push declaration on stack, referenced by addressof. */
    mov r0, #1                               /* Evaluate Expression */
    mov sp, fp
    pop { fp }
    bx lr
