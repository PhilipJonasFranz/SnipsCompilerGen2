SOURCE
struct X<T> {
	T value;
	
	/* Getter for field value */
	T getValue<T>() {
		return self->value;
	}
}

int main() {
	X<int> x = X<int>::(10);
	x.getValue<int>();
}
TESTCASES
10
OUTPUT
/* --test_02.txt */
    b main
X.getValue_P0:                               /* Function: X.getValue, Provisos: INT */
    lsl r1, r0, #2                           /* Convert to bytes */
    ldr r0, [r1, #4]                         /* Load field from struct */
    bx lr
    
main:                                        /* Function: main */
    push { fp, lr }
    mov fp, sp
    mov r0, #10                              /* Evaluate Expression */
    push { r0 }
    mov r0, #1
    push { r0 }
    sub r0, fp, #8                           /* Load parameters */
    lsr r0, r0, #2
    bl X.getValue_P0                         /* Call X.getValue */
    mov sp, fp
    ldmfd sp!, {fp, pc}
    
