DESCRIPTION
This test verifies global variable initialization.
SOURCE
struct S {
	int x;
	int y;
}

bool sw = true;

S s0 = S::(10, 5);

S s1 = (sw)? S::(s0.y, 2) : s0;

int main(int a) {
	return a * (s0.x + s0.y);
}
TESTCASES
0 0
1 15
-2 -30
OUTPUT
.version 16738167287

.include maybe __op_div.s
.include maybe __op_mod.s
.include maybe resv.s
.include maybe isa.s
.include maybe init.s
.include maybe hsize.s
.include maybe free.s

.data
S: .word 0
NULL: .word 0
HEAP_START: .word 0
s1: .skip 12
s0: .skip 12
sw: .word 1

.text
    b main_init

/* System Routine, used to copy memory on the stack */
_routine_stack_copy_: 
    cmp r0, #0
    beq _routine_stack_copy_end_
    ldr r2, [r1, #-4]!
    sub r0, r0, #4
    push { r2 }
    b _routine_stack_copy_
_routine_stack_copy_end_: 
    mov pc, r10

.global main

/* Initialize the global variables */
main_init: 
    sub sp, sp, #12
    stmea sp, {r0-r2}
    mov r0, #5
    mov r1, #10
    stmfd sp!, {r0, r1}
    ldr r0, .P308228756_S
    ldr r1, .P308228756_s0                   /* Load from .data section */
    str r0, [r1]
    pop { r0 }
    str r0, [r1, #4]
    pop { r0 }
    str r0, [r1, #8]
    ldr r0, .P308228756_sw                   /* Load from .data section */
    ldr r0, [r0]
    cmp r0, #0
    beq main.L2
    mov r0, #2
    push { r0 }
    ldr r1, .P308228756_s0                   /* Load field location */
    ldr r0, [r1, #8]                         /* Load field from struct */
    push { r0 }
    ldr r0, .P308228756_S
    push { r0 }
    b main.L3
main.L2: 
    ldr r2, .P308228756_s0                   /* Load data section address */
    add r2, r2, #8
    ldr r0, [r2] #-4
    ldr r1, [r2] #-4
    stmfd sp!, {r0, r1}
main.L3: 
    pop { r0 }
    ldr r1, .P308228756_s1                   /* Load from .data section */
    str r0, [r1]
    pop { r0 }
    str r0, [r1, #4]
    pop { r0 }
    str r0, [r1, #8]
    ldmfd sp!, {r0-r2}
main:                                        /* Function: main */
    mov r3, r0
    ldr r1, .P308228756_s0                   /* Evaluate Expression */
    ldr r0, [r1, #4]                         /* Load field from struct */
    ldr r1, .P308228756_s0                   /* Load field location */
    ldr r2, [r1, #8]                         /* Load field from struct */
    add r2, r0, r2
    mul r0, r3, r2
    bx lr
.P308228756_s1: .word s1
.P308228756_sw: .word sw
.P308228756_s0: .word s0
.P308228756_S: .word S
