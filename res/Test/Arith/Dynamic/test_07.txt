SOURCE
int main(int a) {
	return (a * 2) + 5 + (a - (a * a));
}
TESTCASES
4 1
OUTPUT
/* --test_07.txt */
main:                                        /* Function: main */
    mov r3, r0
    mov r2, #2
    mul r1, r0, r2
    add r1, r1, #5
    mul r2, r3, r3
    sub r2, r3, r2
    add r0, r1, r2
    bx lr
    
