SOURCE
int bar() {
	return 6;
}

int foo() {
	return bar() * bar();
}

int main() {
	return foo() + foo();
}
TESTCASES
72
OUTPUT
/* --test_01.txt */
    b main
bar:                                         /* Function: bar */
    mov r0, #6                               /* Evaluate Expression */
    bx lr
    
foo:                                         /* Function: foo */
    push { fp, lr }
    mov fp, sp
    bl bar                                   /* Evaluate Expression */
    push { r0 }
    bl bar                                   /* Call bar */
    pop { r1 }
    mul r0, r1, r0
    mov sp, fp
    pop { fp, lr }
    bx lr
    
main:                                        /* Function: main */
    push { fp, lr }
    mov fp, sp
    bl foo                                   /* Evaluate Expression */
    push { r0 }
    bl foo                                   /* Call foo */
    pop { r1 }
    add r0, r1, r0
    mov sp, fp
    pop { fp, lr }
    bx lr
    
