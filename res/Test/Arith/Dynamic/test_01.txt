SOURCE
int bar() {
	return 6;
}

int foo() {
	return bar() * bar();
}

int main() {
	return foo() + foo();
}
TESTCASES
72
OUTPUT
/* --test_01.txt */
    b main
bar:                                         /* Function: bar */
    mov r0, #6                               /* Evaluate Expression */
    bx lr
    
foo:                                         /* Function: foo */
    push { lr }
    bl bar                                   /* Evaluate Expression */
    push { r0 }
    bl bar                                   /* Call bar */
    pop { r1 }
    mul r0, r1, r0
    pop { pc }
    
main:                                        /* Function: main */
    push { lr }
    bl foo                                   /* Evaluate Expression */
    push { r0 }
    bl foo                                   /* Call foo */
    pop { r1 }
    add r0, r1, r0
    pop { pc }
    
