SOURCE
int bar(int x) {
	return x;
}

int foo() {
	return bar(8) * bar(8);
}

int main() {
	return foo() + foo();
}
TESTCASES
128
OUTPUT
/* --test_02.txt */
    b main
bar:                                         /* Function: bar */
    bx lr
    
foo:                                         /* Function: foo */
    push { lr }
    mov r0, #8                               /* Evaluate Expression */
    bl bar                                   /* Call bar */
    push { r0 }
    mov r0, #8                               /* Load parameters */
    bl bar                                   /* Call bar */
    pop { r1 }
    mul r0, r1, r0
    pop { pc }
    
main:                                        /* Function: main */
    push { lr }
    bl foo                                   /* Evaluate Expression */
    push { r0 }
    bl foo                                   /* Call foo */
    pop { r1 }
    add r0, r1, r0
    pop { pc }
    
