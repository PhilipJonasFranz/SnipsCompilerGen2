SOURCE
int main() {
	return (1 >> 1 << 1) + (1 << 1 >> 1);
}
TESTCASES
1
OUTPUT
/* --test_02.txt */
main:                                        /* Function: main */
    mov r0, #0
    push { r0 }
    mov r2, #1
    pop { r1 }
    add r0, r1, #1
    bx lr
    
