SOURCE
struct Nib<T> {
	T value;
}

struct Data<T> {
	Nib<T> n0;
	Nib<T> n1;
}

int main() {
	Data<int> data = Data<int>::(Nib<int>::(5), Nib<int>::(10));
	return data.n0.value + data.n1.value;
}
TESTCASES
15
OUTPUT
/* --test_06.txt, Snips Version: v.3.3.1, Date: 06/25/2020 22:19:53 */
/*  SID-Headers: Enabled, Optimizer: Enabled, Modifiers: Enabled */
main:                                        /* Function: main */
    mov fp, sp
    mov r0, #10                              /* Evaluate Expression */
    push { r0 }
    mov r0, #1
    push { r0 }
    mov r0, #5
    push { r0 }
    mov r0, #1
    push { r0 }
    mov r0, #2
    push { r0 }
    sub r1, fp, #20                          /* Evaluate Expression */
    add r1, r1, #8
    ldr r0, [r1]                             /* Load field from struct */
    push { r0 }
    sub r1, fp, #20                          /* Load field location */
    add r1, r1, #16
    ldr r2, [r1]                             /* Load field from struct */
    pop { r1 }
    add r0, r1, r2
    bx lr
    
