SOURCE
struct Nib<T> {
	T* value;
}

struct Data<T> {
	Nib<T> n0;
	Nib<T> n1;
}

int main() {
	int x = 19;
	int y = 11;
	Data<int> data = Data<int>::(Nib<int>::(&x), Nib<int>::(&y));
	return *(data.n0.value) + *(data.n1.value);
}
TESTCASES
30
OUTPUT
/* --test_09.txt */
main:                                        /* Function: main */
    push { r3 }
    mov fp, sp
    mov r3, #19
    push { r3 }                              /* Push declaration on stack, referenced by addressof. */
    mov r3, #11
    push { r3 }                              /* Push declaration on stack, referenced by addressof. */
    sub r0, fp, #8                           /* Evaluate Expression */
    lsr r0, r0, #2
    push { r0 }
    mov r0, #1
    push { r0 }
    sub r0, fp, #4
    lsr r0, r0, #2
    push { r0 }
    mov r0, #1
    push { r0 }
    mov r0, #2
    push { r0 }
    sub r1, fp, #28                          /* Evaluate Expression */
    add r1, r1, #8
    ldr r0, [r1]                             /* Load field from struct */
    lsl r0, r0, #2                           /* Convert to bytes */
    ldr r0, [r0]                             /* Load from address */
    push { r0 }
    sub r1, fp, #28                          /* Load field location */
    add r1, r1, #16
    ldr r0, [r1]                             /* Load field from struct */
    lsl r0, r0, #2                           /* Convert to bytes */
    ldr r2, [r0]                             /* Load from address */
    pop { r1 }
    add r0, r1, r2
    mov sp, fp
    pop { r3 }
    bx lr
    
