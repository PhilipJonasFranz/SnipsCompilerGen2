SOURCE
int main(int b) {
	int* p = &b;
	*p += 5;
	return b;
}
TESTCASES
0 5
4 9
-2 3
OUTPUT
/* --test_02.txt, Snips Version: v.3.3.1, Date: 06/25/2020 22:19:50 */
/*  SID-Headers: Enabled, Optimizer: Enabled, Modifiers: Enabled */
main:                                        /* Function: main */
    push { r3 }
    mov fp, sp
    mov r3, r0
    push { r3 }                              /* Push declaration on stack, referenced by addressof. */
    sub r0, fp, #4                           /* Evaluate Expression */
    lsr r0, r0, #2
    mov r3, r0
    lsl r1, r0, #2
    ldr r2, [r1]
    add r0, r2, #5
    str r0, [r1]
    ldr r0, [fp, #-4]                        /* Evaluate Expression */
    mov sp, fp
    pop { r3 }
    bx lr
    
