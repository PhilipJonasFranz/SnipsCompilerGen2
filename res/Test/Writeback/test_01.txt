SOURCE
int main(int a) {
	int x = 5 + a++ + a++;
	return a;
}
TESTCASES
0 2
-1 1
2 4
OUTPUT
/* --test_01.txt */
main:                                        /* Function: main */
    mov r3, r0
    add r3, r0, #1
    add r1, r0, #5
    mov r0, r3
    add r3, r0, #1
    add r4, r1, r0
    mov r0, r3                               /* Evaluate Expression */
    bx lr
    
