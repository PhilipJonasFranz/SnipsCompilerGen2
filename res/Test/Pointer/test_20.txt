SOURCE
void set(int*[2] p) {
		/* Set m [0] [1] */
	*(p [0] + 1) = 8;
	
		/* Set m [1] [0] */
	*p[1] = 7;
}

int main() {
	int [2] [2] m = {{3, 6}, {1, 2}};
	int*[2] p = {&m[0], &m[1]};
	set(p);
	return m [0] [1] + m [1] [1];
}
TESTCASES
10
OUTPUT
/* --test_20.txt, Snips Version: v.3.3.1, Date: 06/25/2020 22:19:52 */
/*  SID-Headers: Enabled, Optimizer: Enabled, Modifiers: Enabled */
    b main
set:                                         /* Function: set */
    push { fp }
    mov fp, sp
/* Set m [0] [1] */
    mov r0, #8                               /* Evaluate Expression */
    push { r0 }
    add r0, fp, #4
    ldr r1, [r0]
    add r0, r1, #1
    lsl r1, r0, #2
    pop { r0 }
    str r0, [r1]
/* Set m [1] [0] */
    mov r0, #7                               /* Evaluate Expression */
    push { r0 }
    add r0, fp, #4
    add r0, r0, #4
    ldr r0, [r0]
    lsl r1, r0, #2
    pop { r0 }
    str r0, [r1]
    mov sp, fp
    pop { fp }
    add sp, sp, #8
    bx lr
    
main:                                        /* Function: main */
    push { fp, lr }
    mov fp, sp
    mov r0, #2                               /* Evaluate Expression */
    mov r1, #1
    push { r1, r0 }
    mov r0, #6
    mov r1, #3
    push { r1, r0 }
    mov r2, #0                               /* Evaluate Expression */
    mov r0, #1
    mov r1, #8
    mul r0, r0, r1
    add r2, r2, r0
    sub r0, fp, #16
    add r0, r0, r2                           /* Add structure offset */
    lsr r0, r0, #2
    push { r0 }
    mov r2, #0                               /* Calculate offset of sub structure */
    mov r0, #0
    mov r1, #8
    mul r0, r0, r1
    add r2, r2, r0
    sub r0, fp, #16
    add r0, r0, r2                           /* Add structure offset */
    lsr r0, r0, #2
    push { r0 }
    ldr r0, [fp, #-20]                       /* Load parameters */
    ldr r1, [fp, #-24]
    push { r1, r0 }
    bl set                                   /* Call set */
    mov r2, #0                               /* Evaluate Expression */
    mov r0, #0
    mov r1, #8
    mul r0, r0, r1
    add r2, r2, r0
    add r2, r2, #4
    sub r0, fp, #16
    add r0, r0, r2
    ldr r0, [r0]
    push { r0 }
    mov r2, #0                               /* Calculate offset of sub structure */
    mov r0, #1
    mov r1, #8
    mul r0, r0, r1
    add r2, r2, r0
    add r2, r2, #4
    sub r0, fp, #16
    add r0, r0, r2
    ldr r2, [r0]
    pop { r1 }
    add r0, r1, r2
    mov sp, fp
    pop { fp, lr }
    bx lr
    
