DESCRIPTION
Combine the elements parameters in stack, local data in stack.
SOURCE
int main(int a, int b, int c, int d) {
	int b0 = a + 0;
	int b1 = a + 1;
	int b2 = b + 2;
	int b3 = b + 3;
	int b4 = c + 4;
	int b5 = c + 5;
	int b6 = d + 6;
	int b7 = d + 7;
	return b0 + b1 + b2 + b3 + b4 + b5 + b6 + b7;
}
TESTCASES
0 0 0 0 0 28
1 2 3 4 48
OUTPUT
/* --test_07.txt */
main:                         /* Function: main */
    push { r3, r4, r5, r6, r7, r8, r9, fp }
    mov fp, sp
    mov r3, r0
    mov r4, r1
    mov r5, r2
    add r6, r3, #0
    add r7, r3, #1
    add r8, r4, #2
    add r9, r4, #3
    add r0, r5, #4
    push { r0 }
    add r0, r5, #5
    push { r0 }
    ldr r1, [fp, #32]         /* Evaluate Expression */
    add r0, r1, #6
    push { r0 }
    ldr r1, [fp, #32]         /* Evaluate Expression */
    add r0, r1, #7
    push { r0 }
    add r1, r6, r7
    add r1, r1, r8
    add r1, r1, r9
    ldr r2, [fp, #-4]
    add r1, r1, r2
    ldr r2, [fp, #-8]
    add r1, r1, r2
    ldr r2, [fp, #-12]
    add r1, r1, r2
    ldr r2, [fp, #-16]
    add r0, r1, r2
    mov sp, fp
    pop { r3, r4, r5, r6, r7, r8, r9, fp }
    add sp, sp, #4
    bx lr
    
