DESCRIPTION
This test aims to verify the correct functionality of assignments to read and write from
and to the stack.
SOURCE
int main(int a, int b) {
	int b0 = a + b;
	int b1 = a + b + 1;
	int b2 = a + b + 2;
	int b3 = a + b + 3;
	int b4 = a + b + 4;
	int b5 = a + b + 5;
	int b6 = a + b + 6;
	int b7 = a + b + 7;
	int b8 = a + b + 8;
	int b9 = a + b;
	
	b9 = a + b9;
	b8 = 8;
	
	return b8 + b9;
}
TESTCASES
0 0 8
1 4 14
4 1 17
OUTPUT
/* --test_12.txt */
main:                                        /* Function: main */
    push { r3, r4, r5, r6, r7, r8, r9 }
    mov fp, sp
    mov r3, r0
    mov r4, r1
    add r5, r3, r4
    add r1, r3, r4
    add r6, r1, #1
    add r1, r3, r4
    add r7, r1, #2
    add r1, r3, r4
    add r8, r1, #3
    add r1, r3, r4
    add r9, r1, #4
    add r1, r3, r4
    add r0, r1, #5
    push { r0 }
    add r1, r3, r4
    add r0, r1, #6
    push { r0 }
    add r1, r3, r4
    add r0, r1, #7
    push { r0 }
    add r1, r3, r4
    add r0, r1, #8
    push { r0 }
    add r0, r3, r4
    push { r0 }
    mov r1, r3                               /* Evaluate Expression */
    ldr r2, [fp, #-20]
    add r0, r1, r2
    str r0, [fp, #-20]
    mov r0, #8                               /* Evaluate Expression */
    str r0, [fp, #-16]
    ldr r1, [fp, #-16]                       /* Evaluate Expression */
    ldr r2, [fp, #-20]
    add r0, r1, r2
    mov sp, fp
    pop { r3, r4, r5, r6, r7, r8, r9 }
    bx lr
    
