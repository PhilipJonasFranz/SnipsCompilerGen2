DESCRIPTION
This test aims to verify the correct functionality of assignments to read and write from
and to the stack.
SOURCE
int main(int a, int b) {
	int b0 = a + b;
	int b1 = a + b + 1;
	int b2 = a + b + 2;
	int b3 = a + b + 3;
	int b4 = a + b + 4;
	int b5 = a + b + 5;
	int b6 = a + b + 6;
	int b7 = a + b + 7;
	int b8 = a + b + 8;
	int b9 = a + b;
	
	b9 = a + b9;
	b8 = 8;
	
	return b8 + b9;
}
TESTCASES
0 0 8
1 4 14
4 1 17
OUTPUT
/* --test_12.txt */
main:                                        /* Function: main */
    mov r3, r0
    mov r4, r1
    add r1, r0, r1
    add r0, r1, #5
    add r1, r3, r4
    push { r0 }
    add r0, r1, #6
    add r1, r3, r4
    push { r0 }
    add r0, r1, #7
    add r1, r3, r4
    push { r0 }
    add r0, r1, #8
    push { r0 }
    add r0, r3, r4
    push { r0 }
    ldr r2, [sp, #0]
    add r0, r3, r2
    str r0, [sp, #0]
    mov r0, #8                               /* Evaluate Expression */
    str r0, [sp, #4]
    ldr r1, [sp, #4]                         /* Evaluate Expression */
    ldr r2, [sp, #0]
    add r0, r1, r2
    bx lr
    
