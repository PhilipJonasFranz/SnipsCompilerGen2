SOURCE
int main() {
	int [2] a = {12, 4};
	a = {15, 24};
	
	return a [0] + a [1];
}
TESTCASES
39
OUTPUT
/* --test_16.txt */
main:                                        /* Function: main */
    mov fp, sp
    mov r0, #4                               /* Evaluate Expression */
    mov r1, #12
    stmfd sp!, {r0, r1}
    mov r0, #24                              /* Evaluate Expression */
    mov r1, #15
    stmfd sp!, {r0, r1}
    pop { r0 }
    sub r1, fp, #8                           /* Start of structure in stack */
    str r0, [r1]
    pop { r0 }
    str r0, [r1, #4]
    ldr r0, [fp, #-8]
    ldr r2, [fp, #-4]
    add r0, r0, r2
    bx lr
     
