SOURCE
int bar(int b) {
	return 3 + b;
}

int foo(int x) {
	return x * 2;
}

int main(int a, int b) {
	return foo(b) + bar(a);
}
TESTCASES
0 0 3
1 4 12
3 5 16
OUTPUT
/* --test_06.txt */
    b main
bar:                                         /* Function: bar */
    add r0, r0, #3
    bx lr
    
foo:                                         /* Function: foo */
    mov r2, #2
    mul r0, r0, r2
    bx lr
    
main:                                        /* Function: main */
    push { lr }
    mov r3, r0
    mov r4, r1
    mov r0, r4                               /* Evaluate Expression */
    bl foo                                   /* Call foo */
    push { r0 }
    mov r0, r3                               /* Load parameters */
    bl bar                                   /* Call bar */
    pop { r1 }
    add r0, r1, r0
    pop { lr }
    bx lr
    
