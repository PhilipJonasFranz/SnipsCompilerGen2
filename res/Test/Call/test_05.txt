SOURCE
int bar(int b) {
	int b0 = b + 2;
	return b0;
}

int foo(int x) {
	int b = bar(x);
	int r = x + b;
	return r;
}

int main(int a) {
	a = a + foo(a + 4);
	return a;
}
TESTCASES
3 19
0 10
5 25
OUTPUT
/* --test_05.txt */
    b main
bar:                                         /* Function: bar */
    push { r3, r4 }
    mov r3, r0
    add r4, r0, #2
    mov r0, r4                               /* Evaluate Expression */
    pop { r3, r4 }
    bx lr
    
foo:                                         /* Function: foo */
    push { r3, r4, r5, fp, lr }
    mov fp, sp
    mov r3, r0
    bl bar                                   /* Call bar */
    mov r4, r0
    add r5, r3, r0
    mov r0, r5                               /* Evaluate Expression */
    mov sp, fp
    pop { r3, r4, r5, fp, lr }
    bx lr
    
main:                                        /* Function: main */
    push { r3, fp, lr }
    mov fp, sp
    mov r3, r0
    add r0, r0, #4
    bl foo                                   /* Call foo */
    add r3, r3, r0
    mov r0, r3                               /* Evaluate Expression */
    mov sp, fp
    pop { r3, fp, lr }
    bx lr
    
