SOURCE
int foo(int x, int y) {
	return (x * 2) + y;
}

int main() {
	return foo(5, 10);
}
TESTCASES
20
OUTPUT
/* --test_07.txt */
    b main
foo:                                         /* Function: foo */
    push { r4 }
    mov r4, r1
    lsl r1, r0, #1
    add r0, r1, r4
    pop { r4 }
    bx lr
    
main:                                        /* Function: main */
    mov r1, #10
    mov r0, #5
    b foo                                    /* Call foo */
    
