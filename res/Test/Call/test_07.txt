SOURCE
int foo(int x, int y) {
	return (x * 2) + y;
}

int main() {
	return foo(5, 10);
}
TESTCASES
20
OUTPUT
/* --test_07.txt */
    b main
foo:                                         /* Function: foo */
    push { r3, r4 }
    mov r3, r0
    mov r4, r1
    mov r1, r3                               /* Evaluate Expression */
    mov r2, #2
    mul r0, r1, r2
    add r0, r0, r4
    pop { r3, r4 }
    bx lr
    
main:                                        /* Function: main */
    push { fp, lr }
    mov fp, sp
    mov r1, #10
    mov r0, #5
    bl foo                                   /* Call foo */
    mov sp, fp
    pop { fp, lr }
    bx lr
    
