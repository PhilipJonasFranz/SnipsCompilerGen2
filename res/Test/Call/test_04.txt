SOURCE
int bar(int b) {
	return b + 2;
}

int foo(int x) {
	return x + bar(x);
}

int main(int a) {
	a = a + foo(a + 4);
	return a;
}
TESTCASES
3 19
0 10
5 25
OUTPUT
/* --test_04.txt */
    b main
bar:                                         /* Function: bar */
    add r0, r0, #2
    bx lr
    
foo:                                         /* Function: foo */
    push { r3, lr }
    mov r3, r0
    bl bar                                   /* Call bar */
    add r0, r3, r0
    ldmfd sp!, {R3, PC}
    
main:                                        /* Function: main */
    push { lr }
    mov r3, r0
    add r0, r0, #4
    bl foo                                   /* Call foo */
    add r3, r3, r0
    mov r0, r3                               /* Evaluate Expression */
    pop { pc }
    
