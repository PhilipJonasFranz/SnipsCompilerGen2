SOURCE
struct Data<T> {
	T v;
}

int main() {
	int [2] q = {1, 4};
	Data<int*> data = Data<int*>::(&q);
	int* p = data.v;
	return *(p + 1);
}
TESTCASES
4
OUTPUT
/* --test_04.txt, Snips Version: v.3.3.1, Date: 06/25/2020 22:19:52 */
/*  SID-Headers: Enabled, Optimizer: Enabled, Modifiers: Enabled */
main:                                        /* Function: main */
    push { r3 }
    mov fp, sp
    mov r0, #4                               /* Evaluate Expression */
    mov r1, #1
    push { r1, r0 }
    sub r0, fp, #8                           /* Evaluate Expression */
    lsr r0, r0, #2
    push { r0 }
    mov r0, #1
    push { r0 }
    sub r1, fp, #16                          /* Evaluate Expression */
    add r1, r1, #4
    ldr r3, [r1]                             /* Load field from struct */
    add r0, r3, #1
    lsl r0, r0, #2                           /* Convert to bytes */
    ldr r0, [r0]                             /* Load from address */
    mov sp, fp
    pop { r3 }
    bx lr
    
